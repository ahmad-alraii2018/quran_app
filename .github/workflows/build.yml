name: Pure Android Cloud Bundle Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Source Code
      uses: actions/checkout@v4

    - name: Smart Unzip & Core Clean
      run: |
        # فك ضغط الحزمة الموسوعية النظيفة المتواجدة بالمستودع
        unzip -q quran_final_2026.zip || unzip -q quran_app.zip
        rm -rf platforms/android plugins/ node_modules/ package-lock.json

    - name: Set up Java JDK 17 Environment
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Set up Android SDK & Accept Licenses
      uses: android-actions/setup-android@v3

    - name: Install Pure Cordova CLI
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm install -g cordova

    - name: Generate Fresh Android Platform
      run: |
        # بناء منصة أندرويد نظيفة ومعزولة داخل السحاب
        cordova platform add android --noscript

    - name: Compile and Build Official App Bundle Only
      run: |
        # الأمر الإمبراطوري الصافي لبناء حزمة الـ AAB الرسمية مجاناً وبدون أخطاء
        cordova build android --release --device

    - name: Upload Finished Packages to Cloud Storage
      uses: actions/upload-artifact@v4
      with:
        name: quran-app-final-packages
        path: platforms/android/app/build/outputs/bundle/release/*.aab
